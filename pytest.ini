[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: needs a live Redis/Postgres or real network (deselected by default)
filterwarnings =
    ignore::DeprecationWarning
